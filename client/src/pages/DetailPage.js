import React, {useState} from 'react';

const DetailPage = () => {
    const [link, setLink] = useState(null);

    return (
        <div>
            <h1>Detail page</h1>
        </div>
    );
};

export default DetailPage;
